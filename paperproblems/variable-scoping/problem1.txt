What is the output of this program?

let x = 1;

function f() {
    x = x * 2;
    console.log(x);
}

f();
f();

2
4

The first execution of the function sets x to a new value of 2. The subsequent execution takes the updated value of x.